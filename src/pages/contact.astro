---
import Base from '../layouts/Base.astro';
---
<Base title="Contact | Sovrin Group" section="contact">
  <div class="dark-zone page-dark">
  <section class="hero-page">
    <div class="container">
      <p class="kicker">Contact</p>
      <h1>We'll call you back at your convenience.</h1>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <form class="contact-form" id="callback-form">
        <label>
          First &amp; Last Name
          <input type="text" name="name" required autocomplete="name" />
        </label>

        <label>
          Organization
          <input type="text" name="organization" autocomplete="organization" />
        </label>

        <div class="form-row">
          <label>
            Preferred Callback Number
            <input type="tel" name="phone" placeholder="(xxx) xxx-xxxx" required autocomplete="tel" />
          </label>
          <label>
            Preferred Callback Day / Time
            <input type="text" name="callback_time" placeholder="mm/dd, hh:mm" />
          </label>
        </div>

        <label>
          Email
          <input type="email" name="email" required autocomplete="email" />
        </label>

        <button class="btn" type="submit">Send</button>

        <p class="form-status" id="callback-status" role="status" aria-live="polite"></p>
      </form>
    </div>
  </section>
  </div>

  <style>
    .form-status {
      margin: 0;
      font-size: 0.92rem;
      line-height: 1.55;
      color: var(--text-on-dark);
      border-top: 1px solid rgba(160, 190, 240, 0.2);
      padding-top: 1.1rem;
    }

    .form-status:empty { display: none; }
  </style>

  <script>
    const form = document.getElementById('callback-form');
    const status = document.getElementById('callback-status');

    if (form && status) {
      form.addEventListener('submit', (event) => {
        event.preventDefault();

        const data = new FormData(form);
        const field = (key) => String(data.get(key) ?? '').trim();

        const body = [
          'Name: ' + field('name'),
          'Organization: ' + (field('organization') || 'not given'),
          'Preferred callback number: ' + field('phone'),
          'Preferred callback day / time: ' + (field('callback_time') || 'no preference'),
          'Email: ' + field('email'),
        ].join('\r\n');

        const href =
          'mailto:info@sovringroup.com' +
          '?subject=' + encodeURIComponent('Callback request: ' + field('name')) +
          '&body=' + encodeURIComponent(body + '\r\n');

        window.location.href = href;

        status.textContent =
          'Your email program should now open with these details filled in. ' +
          'Send that message and we will call you back at the time you asked for. ' +
          'If nothing opened, write to info@sovringroup.com instead.';
      });
    }
  </script>
</Base>
